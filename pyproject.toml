[project]
name = "enterprise-rag-assistant"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools]
packages = [
    "app",
    "core",
    "ingestion",
    "retrieval",
    "answer_generation",
    "observability",
    "evals",
]

[tool.setuptools.package-dir]
"" = "."
